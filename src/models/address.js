export default (methodType, streetName, ward, district, city, country) => {
  return {
    MethodType: methodType,
    StreetName: streetName,
    Ward: ward,
    District: district,
    City: city,
    Country: country
  };
};
